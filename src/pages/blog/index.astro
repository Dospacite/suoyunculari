---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getBlogPosts } from '@/lib/directus';

export const prerender = false;

const posts = await getBlogPosts();
---

<BaseLayout title="Blog" description="SUOyuncuları duyuruları, prova notları ve kulüp yazıları.">
  <main class="section compact">
    <div class="section-inner">
      <div class="section-heading">
        <h1>Blog</h1>
      </div>
      <div class="article-list">
        {
          posts.map((post) => (
            <a href={`/blog/${post.slug}`}>
              {post.author_name && <p class="kicker">{post.author_name}</p>}
              <h2>{post.title}</h2>
              {post.excerpt && <p>{post.excerpt}</p>}
            </a>
          ))
        }
      </div>
    </div>
  </main>
</BaseLayout>
