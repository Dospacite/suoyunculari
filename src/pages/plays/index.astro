---
import PlayList from '@/components/PlayList.astro';
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getPlays } from '@/lib/directus';

export const prerender = false;

const plays = await getPlays();
---

<BaseLayout title="Oyunlarımız" description="SUO Tiyatro Kulübü oyun arşivi.">
  <main class="section compact">
    <div class="section-inner">
      <div class="section-heading">
        <p class="kicker">Kulüp Arşivi</p>
        <h1>Oyunlarımız</h1>
        <p>SUO tarafından sahnelenen ve arşivlenen oyunları başlık, yazar, tür, zorluk ve hak durumuna göre ara.</p>
      </div>
      <PlayList plays={plays} />
    </div>
  </main>
</BaseLayout>
